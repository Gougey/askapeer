# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# Claude Code: personal/session files stay ignored; the shared project
# settings (permission allowlist) are checked in per standard convention
.claude/*
!.claude/settings.json

# Node (for when the real build lands)
node_modules/
dist/
.env
.env.*
!.env.example

# Next.js / build output
.next/
out/
*.tsbuildinfo
coverage/

# Microsoft Office temp/lock files
~$*
